// get_route
// server calls function routes (route_name) => checks a dictionary if route is present
    // if yes:
        // get html file related to route and return as body
    // if no:
        // send 404 

// make_route
// prereq: make dictionary
// args(route_name, route_file) 
    // => adds route_name to dictionary and links with route_file